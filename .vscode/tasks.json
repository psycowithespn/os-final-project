{
    "tasks": [
        {
            "type": "shell",
            "label": "Compile - Pthread",
            "command": "clang",
            "args": [
                "-pthread",
                "-g",
                "*.c"
            ],
            "options": {
                "cwd": "${workspaceFolder}",
            }
        }
    ],
    "version": "2.0.0"
}